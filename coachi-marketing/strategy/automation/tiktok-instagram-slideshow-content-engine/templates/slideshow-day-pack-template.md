# Slideshow Day Pack - YYYY-MM-DD

## Theme
`<one runner problem>`

## Platform
- TikTok slideshow
- Instagram carousel or Reel-style slideshow

## Objective
- `<recognition / saves / comments / profile clicks>`

## Hook
`<6 to 12 words when needed to preserve the question, contradiction, number, or runner pain>`

## Core Idea
- Problem:
- Emotion:
- Pattern:
- Visual world:
- Avatar brief:

## Hook Bank
Add `30` hooks before selecting the final hook.

Required formats:
- list-based
- how-to
- curiosity
- contradiction / myth breaking

Rules:
- use `inputs/research/tiktok-proven-slideshow-text-bank.json` first
- use TikTok-observed structures from `inputs/research/tiktok-running-hook-pattern-bank.md`
- do not mention `data` in hooks
- do not copy exact creator wording

## Top 5 Hooks
1. `<hook>` - `<why it works>`
2. `<hook>` - `<why it works>`
3. `<hook>` - `<why it works>`
4. `<hook>` - `<why it works>`
5. `<hook>` - `<why it works>`

## Slide Sequence
1. `<hook>`
2. `<problem>`
3. `<why it happens>`
4. `<reframe>`
5. `<practical rule>`
6. `<optional Coachi tie-in>`
7. `<comment prompt>`

Required spine:
- Slide 1 uses one Images 2.0 generated Coachi runner avatar hook image.
- Slide 1 hook and slides 1-6 use the source-backed TikTok slideshow text bank where possible.
- The selected emotion must appear in the hook, setup, or reframe.
- Slides 2-7 use Supabase/library assets from the same visual world and similar lighting.
- Slide 7 is always the CTA slide and asks for one simple action.

## Image Prompts
Default visual mapping:
- Slide 1 -> Images 2.0 generated avatar hook in the selected world
- Slide 2 -> same-world context
- Slide 3 -> same-world effort/problem
- Slide 4 -> same-world emotional detail
- Slide 5 -> same-world calm/rule
- Slide 6 -> same-world reframe or Coachi tie-in
- Slide 7 -> same-world CTA or clean CTA background that does not break the world

Visual rules:
- Do not mix hills, lakes, and mountains in one slideshow.
- Keep lighting similar across the deck.
- Rotate Supabase/library assets and avoid reusing the same visual set on consecutive slideshows.

### Slide 1
`<prompt>`

### Slide 2
`<prompt>`

### Slide 3
`<prompt>`

### Slide 4
`<prompt>`

### Slide 5
`<prompt>`

### Slide 6
`<prompt>`

### Slide 7
`<prompt>`

## TikTok Caption
```text
<short caption>
```

## Instagram Caption
```text
<slightly deeper caption>
```

## Hashtags
```text
#running #easyrun #runningtips #beginnerrunner #runtok
```

## QA
- [ ] First slide readable in under 1 second
- [ ] Hook and slides 1-6 are sourced from or shaped by `tiktok-proven-slideshow-text-bank.json`
- [ ] Emotion is explicit and visible in the slide logic
- [ ] Slide 1 is the only Images 2.0 image
- [ ] Generated Coachi runner avatar/world brief is included
- [ ] One visual world and similar lighting across the deck
- [ ] One idea per slide
- [ ] No tiny text
- [ ] No obvious AI artifacts
- [ ] Final slide is a CTA with one simple action
- [ ] Caption matches the slide promise

## Result Log
- TikTok URL:
- Instagram URL:
- 1h views:
- 24h views:
- saves:
- comments:
- follows:
- decision: `repeat / iterate / stop`
